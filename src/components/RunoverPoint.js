var React    = require('react');
var ReactDOM = require('react-dom');

var RunoverPoint = React.createClass({
  
  render: function ()
  {
    return <div className="runover-point"></div>
  }
  
});

module.exports = RunoverPoint;