var React    = require('react');
var ReactDOM = require('react-dom');
var RunoverPoint = require('./RunoverPoint');

var RunoverPoints = React.createClass({
  
  render: function ()
  {
    return <div className="runover-points"></div>
  }
  
});

module.exports = RunoverPoints;